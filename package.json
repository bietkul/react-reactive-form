{
  "name": "reactive-form",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*",
    "packages/react-reactive-form/examples/*"
  ],
  "scripts": {
    "dev": "npm run watch --prefix packages/react-reactive-form"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
